subdir('core')
subdir('platform')
subdir('containers')
subdir('renderer')

internal_inc = include_directories('.')

library_files = [
  core_files,
  platform_files,
  containers_files,
  renderer_files,
]
